// Vercel Serverless Entry Point
// Vercel auto-detects and compiles TypeScript files in /api directory
// All requests are rewritten here via vercel.json rewrites
import app from '../src/app';

export default app;
